<h1>Listing all notes</h1>
<% @notes.each do |note| %>
<p><%= note.title %></p>
<p><%= note.body %></p>
<p><%= link_to 'Delete', note_path(note), method: :delete, data:{confirm:"Are you sure?"} %></p>
<% end %>
