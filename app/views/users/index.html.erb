<h1>My Non Followers</h1>

<% @users.each do |user| %>
  <% if current_user.not_followers_with?(user.id) %>
    <% if current_user.id != user.id %>
      <p>Profile of User_id: <%= user.id %></p>
    <p><%=link_to 'Go to profile', user_path(user.id) %></p>  
    <% end %>
  <% end %>
<% end %>
